candidates:
- promptResponse:
    content:
      card:
        image:
          alt: ???
          url: https://developers.google.com/assistant/assistant_96.png
        subtitle: 'Question # $session.params.round - Your Score : $session.params.score ($user.params.score)'
        text: $session.params.quiz ?
        title: $session.params.operation
    firstSimple:
      variants:
      - speech: Sorry I did not get your answer. $session.params.quiz ?
