candidates:
- promptResponse:
    content:
      card:
        image:
          alt: ???
          url: https://developers.google.com/assistant/assistant_96.png
        subtitle: $session.params.round
        text: '$session.params.quiz ? \nQuestion #$session.params.round'
        title: $session.params.operation
    firstSimple:
      variants:
      - speech: $session.params.quiz ?
